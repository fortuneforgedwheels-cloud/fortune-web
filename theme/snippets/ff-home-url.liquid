{%- comment -%}
  Canonical storefront homepage path.

  Bare "/" is poisoned by Shopify IndexController page cache (can keep serving
  deleted-theme HTML). "/index" renders the live Theme Editor `index.json`
  immediately, including image/video uploads.
{%- endcomment -%}
/index

{%- comment -%}
  REMOVED — do not paste ticket Liquid into Shopify Admin.

  If tickets still appear in Order confirmation email or the order-status
  “Additional scripts” box, delete those pasted blocks in Admin:

  1) Settings → Notifications → Order confirmation → remove any
     “giveaway entry tickets” / paid_entries / ff_ticket / FF_TICKETS block
  2) Settings → Checkout → Order status page → Additional scripts →
     remove any ff-entry-tickets / ticket Liquid block

  Theme code no longer ships ticket paste content.
{%- endcomment -%}
